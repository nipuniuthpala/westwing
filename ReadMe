You need to have installed 
  npm init
  npm install --save-dev @wdio/cli
  npm install --save-dev prettier
  npm install --save-dev @babel/core @babel/cli @babel/preset-env @babel/register
  npm install wdio-docker-service --save-dev
  
You need to download the zip folder (if needed open it in visial studio code)
execute using npm run test  
It will pop up the chrome browser and execute the tests.
======

Also it is dockerized and could execute using following commands
docker-compose build
docker-compose up 
